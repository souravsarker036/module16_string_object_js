const person = {
    name: 'sodor uddin',
    age: 25,
    profession: 'developer',
    salary: 20000,
    married: false
}
// console.log(person) //show all property of object

// console.log(person.age) //it's shown only the valu of object property

const income = person.salary; // property value k onno var a rakha
// console.log(income)

//bracket notation
console.log(person['age'])

const boyos = person['age']
console.log(boyos)